# 核心依赖
requests>=2.31.0
feedparser>=6.0.10
pyyaml>=6.0.1
python-dotenv>=1.0.0
schedule>=1.2.0
python-dateutil>=2.8.2
beautifulsoup4>=4.12.0

# AI依赖
# 说明: 默认启用 openai, 与 config/app.yaml 中 content_enhancer.provider 的默认值保持一致
openai>=1.0.0  # 使用OpenAI兼容API时需要

# 可选依赖(按需安装)
# anthropic>=0.18.0  # 使用Claude API时需要
